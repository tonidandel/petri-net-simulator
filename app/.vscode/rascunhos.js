function adicionar_lugar(array_lugares) {
    n = array_lugares.length
    array_lugares.push(`Ad ${n}`)
    console.log(array_lugares)
}

array_lugares = []
adicionar_lugar(array_lugares)
adicionar_lugar(array_lugares)
adicionar_lugar(array_lugares)
adicionar_lugar(array_lugares)

